<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <style>
        * {
            padding: 0%;
            margin: 0%;
            box-sizing: border-box;
            font-family: Verdana, Geneva, Tahoma, sans-serif;

        }

        header {
            width: 100%;
            height: 60px;
            border-bottom: 2px solid gray;
            display: flex;
            align-items: center;
        }

        header h1 {
            width: 100%;
            text-align: center;
            color: rgb(13, 13, 63);
        }

        #update-det{
            display: flex;
            flex-direction: column;
            border: 1px solid rgb(116, 110, 110, 0.4);
            border-radius: 25px;
            width: 40%;
            margin: auto;
            margin-top: 10px;
            padding: 20px 0;
            min-height: 40px;
        }

        #update-det input, label{
            margin: auto;
            width: 90%;
            height: 30px;
        }
        #update-det input{
            margin-bottom: 20px;
            padding: 10px;
        }
        input::placeholder{
            padding: 10px;
        }
        #update-det button{
            margin: auto;
            width: 130px;
            height: 30px;
            transition: 0.5s;
            border-radius: 5px;
        }
        #update-det button:hover{
            background-color: black;
            color: white;
        }
    </style>
</head>
<body>
    <%- include('partials/_header') %>
    <section class="update">
        <form id="update-det" action="/update/<%- update.id %>" method="post">
            <label for="name">Name</label>
        <input type="text" placeholder="Name" name="name" value="<%- update.name %>" id="name">
        <label for="phone">Mobile Number</label>
        <input type="text" required minlength="10" maxlength="10" pattern="[0-9]{1,10}" placeholder="Mobile Number" name="phone" value ="<%-update.phone %>" id="phone">
        <button>Update</button>
        </form>
    </section>
</body>
</html>